import 'package:flutter/material.dart';

const primaryColor = Color(0xFF343d4d);
const secondaryColor = Color(0xFFc1435f);
const darkColor = Color(0xFF2b323f);
const bodyTextColor = Color(0xFF636363);
const bgColor = Color(0xFFe6e6e6);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1);
const maxWidth = 3840.0;